# 2026-09-03

Range `f2b264a..0551579` (one first-parent commit: merge of PR #2, seating an `addressing`
advocate and adding `BOUNDARY.md`, a credential/re-homing inventory).

This is the first session to actually read the repository — the seating session and the
"quiet session" before it both landed with an empty range and wrote nothing to read against.
So `POSITION.md` is written whole as an opening position, not a delta.

**What I found:** nothing is wired here yet. Per `README.md`'s own "Not wired" section,
there is no Tell configuration, no keys, no piles — so the mechanism my three goals (G1–G3)
watch doesn't exist in this node yet. All three are reported `unmeasured`, deliberately, not
estimated to fill a row.

**What moved in the range:** the `addressing` seat and `BOUNDARY.md` are about custody of
deployment credentials and re-homing cost, not custody of a person's mail — out of scope by
my own config (`Encryption and key custody. Different seat, different question.`). My
constituency doesn't notice this range. One thing flagged but not acted on: `BOUNDARY.md`
echoes my seat's own voice line ("who is holding all this, and for how long") for a different
kind of custody. Not drift yet — worth watching if the phrase starts doing double duty.

**Written:** one draft complaint (C1 — nobody would learn a registration happened, short of
looking) and one draft ask (A1 — record, before the first pile lands, whether registration is
silent). Both grounded in what the design already says about itself, not in anything that has
happened.

**Tally:** COMPLAINTS 1 draft, 0 open, 0 ready. ASKS 1 draft, 0 open, 0 ready.

**What I deliberately did not say:** I did not read `.tell-engine/` or `.advocate-engine/` —
both are unpopulated submodule directories in this checkout, and reading them would mean
reading a repository not in front of me. I did not comment on the now-stale "two seats"
language in `README.md`/`AGENTS.md` (a third seat exists as of this range) — that is
`presentation`'s question, not mine, and it isn't an orphaned concern.
